# Elara

Elara is a WhatsApp bot project retaining the original command flow and file structure while presenting a refreshed, feminine brand identity.

## Configuration

The configured newsletter JID is `120363421014261315@newsletter`. The configured WhatsApp group invite is:

<https://chat.whatsapp.com/HxCDA2s89LMEZMyixnTSy5?s=cl&p=a&mlu=4>

Additional newsletter JIDs can be added when provided by the owner.

## Credentials

For safety, local authentication and bot-token files are excluded from version control through `.gitignore`. Provide `auth.json` and `nexstore/token.js` locally before running the bot.

## Run

Install dependencies and start the bot with:

```bash
npm install
npm start
```
